# Robolectric 4.16.1 supports up to SDK 36; pin tests to it so the compileSdk=37
# bump doesn't fail with "targetSdkVersion=37 > maxSdkVersion=36". Bump once
# Robolectric ships SDK 37 support.
sdk=36
